{
  "name": "collabboard",
  "private": true,
  "description": "End-to-end test harness for the CollabBoard server + client",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
